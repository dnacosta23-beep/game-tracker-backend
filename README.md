# Game-Tracker-Backend

## What this API stores

This API stores player information for online puzzle games in a Supabase table called `game_players`.

Each record includes:
- `id`
- `username`
- `email`
- `game_name`
- `created_at`

## How to run the backend

1. Open the backend folder in the terminal.
2. Activate the virtual environment:

```bash
source .venv/bin/activate